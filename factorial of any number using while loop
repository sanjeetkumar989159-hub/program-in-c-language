#include <stdio.h>

int main() {
    int n, factorial = 1; 


    printf(" enter a number= ");
    scanf("%d", &n); 

    while (n> 0) {
        factorial =n*factorial;
        n--;
    }

    
    printf("Factorial: %d\n", factorial);

    return 0;  
}
